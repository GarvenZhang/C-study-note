//
// Created by Garven on 2018/7/14.
//

#include <stdio.h>

// === 预处理指令: 以 # 开头的语句, 必须在开头 === //
// === 头文件: .h后缀文件, 可以是C标准库中的头文件也可是自定义的库文件 === //
// === main(): C程序处理的入口, 可以返回值也可不返回 === //
// === printf(): 输出一行文字 === //
// === 分号: 必须, 一个分号表示一个语句 === //

void main()
{
    printf("Hello world");
}
