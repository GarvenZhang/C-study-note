//
// Created by Garven on 2018/7/14.
//
#include <stdio.h>

// === 变量: 计算机中一块特定的内存空间, 由一个或多个连续的字节组成 === //
// === 1 变量名: 通过变量名可快速找到内存中存储的数据 === //
// === 2 命名规则: === //
// === 2.1 只能由字母、数字、下划线组成，且第一个字符必须为字母或下划线 === //
// === 2.2 不能使用保留字 === //

void main ()
{
    int num = 5;
    printf(num);
}
